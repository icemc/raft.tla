• No new messages 
• New component switch 
• No distinction between messages, payload and metadata are considered same
• Followers can now respond to client
• Leader Sends Metadata to followers after receiving request to tell which follower should respond to client
• Followers responds to Leader when it receives order message from Leader but it checks if it has received the payload earlier from the switch
• switch keeps it's own log

1. Switch sends payload to every follower including reader
2. Leader prepares metadata and Sends to followers
3. Follower upon receiving the metada checks if client exists in switch (ordered or unordered ?), it received the payload and if all checks are passed then it commits to the leader
4. Define similar function to receive (i, n) where Entry has a new field called payload
5. Define Client request for switch function which initiates 4 client request for the system



---------------------------------------------------------------------------------------------

09/04/2025

Added 

\* Server cache containing all requests sent from Switch to Servers including Leader
VARIABLE serverRequestCache

requestVars == <<serverRequestCache>>

Modified ClientRequest to implicitly serve as switch and forward incoming requests to all servers including leading by appending entries to serverRequestCache[s] where s is the server and serverRequestCache[s] is the Seq of requests sent from switch to server s  

Modified AppendEntries to send appendEntries message containing only metadata from Leader to follower

Modified HandleAppendEntriesRequest to check if follower received request from switch and also checking if incoming entry matches a request before adding it to followers log (including payload)


TODO: Implement recovery mechanism in case entry sent through HandleAppendEntriesRequest is not found in request sent by switch to server i


-----------------------------------------------------------------------

13/04/2025 

Added new invariant to check if followers are able to append entries

FollowerAppendsEntry == \E i \in Server : state[i] = Follower /\ Len(log[i]) = 1 \* Verifies that at least one follower appends an entry

------------------------------------------------------------------------

14/04/2025 

Fixed bug in HandleAppendEntriesRequest function that caused followers not to append entries

changed: IN log' = IF    entries /= << >> THEN log
to: IN log' = IF    entries = << >> THEN log

This made the implementation to work as intended


-----------------------------------------------------------------------------

16/04/2025 

Separation of concerns

1. Refactored ClientRequest to now only serve as switch and forward requests to every server
2. Added LeaderReceivesRequest function which adds received requests to leader log and mark entry as not committed
3. Updated MyNext & Next to reflect the changes made above

------------------------------------------------------------------------------

18/04/2025

Added Recovery mechanisms

1. Implemented HandleRecoveryRequest function which enables leader to add lost request to followers requestCache
2. Follower now sends RecoveryRequest message to leader if it can't find request in it's cache during HandleAppendEntriesRequest call
3. Added possibility to handle RecoveryRequest type messages in Receive function in raftSpec.tla

Refactor code

1. Renamed switchCache to serverRequestCache
2. Renamed switchVars to requestVars

----------------------------------------------------------------------------------

27/04/2024 - 29/04/2024

Changed approach

1. Switch now sends request to servers via message channel
2. Added a new constant called Switch to identify messages sent by the switch
3. Added a new message type NewSwitchRequest which identifies a new request sent by the switch to servers
4. Server handle the NewSwitchRequest in  HandleNewSwitchRequest function
5. Leader upon receiving a new request append it to its log, this is handled in LeaderReceivedRequest function
6. Added new message type RecoveryRequest
7. Server upon receiving an AppendEntriesRequest check if the request from leader in found in their cache else they send RecoveryRequest message to Leader
8. Recovery of request is handled in HandleRecoveryRequest function


TODO test recovery mechanism 